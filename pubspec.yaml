name: oro_high_scanner
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.5
  flutter_dotenv: ^6.0.0
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.1.3
  permission_handler: ^12.0.1
  cupertino_icons: ^1.0.8  # Fixed the spelling
  intl: ^0.20.2
  equatable: ^2.0.5
  file_picker: ^10.3.7
  csv: ^6.0.0
  path_provider: ^2.1.4
  excel: ^4.0.1 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - asset/orositebanner.png
    - asset/orosite.jpg
    - .env